name: Schumann Resonance v6 (Pixel Extraction)

on:
  schedule:
    # 15分ごと
    - cron: "*/15 * * * *"
  workflow_dispatch: # 手動実行も可能

permissions:
  contents: write

jobs:
  fetch:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          pip install pillow numpy

      - name: Fetch and analyze (deterministic pixel extraction)
        run: python fetch_schumann_v6.py

      - name: Commit results
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add schumann_data.json schumann_history.json latest_spectrogram.jpg latest_linegraph.jpg || true
          git diff --staged --quiet || git commit -m "Update Schumann data $(date -u +'%Y-%m-%d %H:%M UTC')"
          git push
